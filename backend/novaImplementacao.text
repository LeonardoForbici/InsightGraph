Você é o desenvolvedor principal do InsightGraph, uma plataforma CAST local 100% offline de análise arquitetural. O projeto já tem tudo da Fase 1 até Fase 9 implementado:
JÁ IMPLEMENTADO (não reimplementar):

Parser AST Java + TypeScript + SQL (via Ollama)
Neo4j + fallback in-memory completo
CALLS_RESOLVED, CALLS_NHOP, CK Metrics, Git Churn, Hotspot Score
RAG local (SQLite + embeddings Ollama) com RagStore persistente
CodeQL integration (database creation, SARIF ingestion, orchestrator)
Impact analysis BFS, Bidirectional analyzer, Taint propagator
Fragility calculator, Side effect detector, Symbol resolver
Dashboard completo: EvolutionDashboard, QualityGatePanel, DebtTrackerPanel
Hotspot Deep Analysis com scatter plot e tabela ordenável
Semantic search panel com modos code/arch/impact
Command Palette (Ctrl+K), NodeQuickActions flutuante, QuickActionToolbar
GraphCanvas 2D com swimlanes, clustering por camada, path finder
GraphCanvas 3D com force-directed + cluster bubbles
Saved Views, Annotations, Tags (SQLite)
API Inventory, Transaction View, Method Usage View
ISO 5055, OSS Exposure, Export CSV/JSON/GraphML


FASE 10 — SECURITY INTELLIGENCE DASHBOARD
10.1 — Painel Central de Segurança
Novo componente SecurityDashboard.tsx acessível via botão na TopBar:
Cards de resumo no topo:

Total de vulnerabilidades (erro/warning/note com cores)
Arquivos com maior concentração de vulnerabilidades
Taxa de cobertura CodeQL (nós com análise vs total)
Nós com is_tainted = true no grafo

Mapa de calor de vulnerabilidades por arquivo:

Grid de retângulos onde tamanho = linhas de código, cor = severidade máxima
Tooltip com: arquivo, projeto, total de vulns, rule_ids encontrados
Clique no retângulo: seleciona o nó no grafo principal

Tabela de vulnerabilidades:

Colunas: Rule ID, Severidade, Arquivo, Linha, Nó afetado, Mensagem
Filtros: severidade, projeto, rule_id (autocomplete)
Ordenação por qualquer coluna
Botão "Ver no grafo" por linha
Export CSV das vulnerabilidades filtradas

10.2 — Correlação Vulnerabilidade × Fragilidade
No mesmo painel, seção "Risco Combinado":

Scatter plot SVG puro: eixo X = fragility_score, eixo Y = vulnerability_count
Cada ponto = um nó; cor = camada arquitetural
Quadrante superior direito destacado como "Zona Crítica"
Top 10 nós com maior risco combinado em tabela abaixo do gráfico

10.3 — Timeline de Análises CodeQL
Novo componente CodeQLTimeline.tsx:

Linha do tempo horizontal mostrando histórico de análises (codeql_history.json)
Cada ponto = uma análise; cor = status (completed/failed)
Hover: mostra duração, suite usada, total de vulnerabilidades encontradas
Clique: abre modal com detalhes completos da análise (já existe AnalysisHistory.tsx, integrar)
Badge "última análise há X dias" no painel CodeQL


FASE 11 — MULTI-PROJECT INTELLIGENCE
11.1 — Comparação entre Projetos
Nova aba "Compare" no Dashboard:

Selecionar 2 projetos via dropdown
Radar chart SVG com 6 eixos: god_classes, circular_deps, dead_code, call_resolution, hotspot_médio, iso_score
Cada projeto = uma cor diferente no radar
Tabela diff abaixo: métrica | projeto A | projeto B | variação (↑↓→)

11.2 — Dependency Graph entre Projetos
Visualizar dependências cross-project no GraphCanvas 2D:

Arestas CONSUMES_API entre projetos com cor diferente (laranja)
Nós de projetos distintos com borda pontilhada por projeto
Filtro "Mostrar apenas dependências cross-project" no sidebar
Tooltip na aresta cross-project mostrando: endpoint, método HTTP, contagem de chamadas inferidas

11.3 — Project Health Score
Novo endpoint GET /api/projects/{name}/health que retorna:
json{
  "project": "meuProjeto",
  "health_score": 72.5,
  "grade": "B",
  "metrics": {
    "god_class_ratio": 0.12,
    "call_resolution_rate": 0.68,
    "avg_hotspot": 24.3,
    "iso_score_percent": 78.0,
    "vuln_count": 3,
    "dead_code_ratio": 0.08
  },
  "trend": "improving" 
}
Card de saúde por projeto no sidebar (badge colorido ao lado do nome do projeto).

FASE 12 — REFACTORING INTELLIGENCE
12.1 — Refactoring Suggestions Engine
Novo endpoint POST /api/refactor/suggest:

Recebe node_key + context (métricas do nó)
Usa Ollama (model complexo) para gerar sugestões estruturadas:

json{
  "suggestions": [
    {
      "type": "extract_method",
      "priority": "high",
      "description": "...",
      "affected_lines": [45, 89],
      "estimated_complexity_reduction": 8
    }
  ]
}

Frontend: novo tab "🔧 Refactoring" no ImpactAnalysisPanel
Exibir sugestões com cards ordenados por prioridade
Botão "Perguntar à IA" em cada sugestão abre AskPanel com contexto pré-carregado

12.2 — Pattern Detector
Detectar automaticamente padrões de design (ou ausência deles):
Backend: novo endpoint GET /api/patterns/detect que analisa o grafo e retorna:

Repository Pattern: classes com sufixo Repository + Entity + Service conectados
Facade Pattern: classe com muitos outgoing CALLS para serviços distintos
God Service: service com WMC > 40 e CBO > 15
Anemic Domain: Entity sem métodos (apenas getters/setters)
Missing Service Layer: Frontend chamando Database diretamente

Frontend: seção "Padrões Detectados" no Dashboard com chips verde (padrão presente) ou vermelho (antipadrão).
12.3 — Change Impact Predictor
Antes de fazer um scan, permitir simular o impacto de refactorings:

Input: "Quero quebrar a classe X em 2 classes"
Output: estimativa de nós afetados, risco de quebra de contratos, esforço estimado
Usa embeddings semânticos + grafo para predição
Endpoint: POST /api/predict/refactor-impact


FASE 13 — DEVELOPER PRODUCTIVITY
13.1 — Git Blame Integration
Para cada nó no NodeDetail, mostrar:

Último committer (já existe _get_git_owner)
Data do último commit no arquivo
Número de autores distintos (indicador de conhecimento compartilhado)
Badge "Bus Factor = 1" quando apenas 1 pessoa tocou o arquivo

13.2 — TODO/FIXME Scanner
No scan, extrair automaticamente comentários TODO, FIXME, HACK, BUG:
Backend: adicionar ao parser Java/TS extração de comentários especiais
python_TODO_PATTERN = re.compile(r'//\s*(TODO|FIXME|HACK|BUG|XXX)\s*:?\s*(.+)', re.IGNORECASE)
Novo endpoint GET /api/todos:
json{
  "items": [
    {"type": "TODO", "text": "...", "file": "...", "line": 42, "node_key": "...", "project": "..."}
  ],
  "total": 87
}
```

Frontend: nova seção no Dashboard "📝 TODOs/FIXMEs" com tabela filtrável por tipo/projeto/arquivo.

### 13.3 — Complexity Trend por Método
Para métodos com histórico Git disponível:

- Calcular como a complexidade ciclomática evoluiu nos últimos 3 meses
- Endpoint: `GET /api/method/{node_key}/complexity-trend`
- Frontend: mini sparkline (SVG 80x24px) no NodeDetail ao lado do valor de complexidade

---

## FASE 14 — PERFORMANCE & SCALABILITY

### 14.1 — Lazy Loading do Grafo
Problema atual: carregar 3871+ nós de uma vez trava o browser.

Implementar paginação do grafo:
- Endpoint `GET /api/graph?page=1&page_size=500` com total_pages
- Frontend: carregar primeiro os nós mais relevantes (maior hotspot_score)
- Botão "Carregar mais 500 nós" no canvas
- Indicador "Exibindo 500/3871 nós (filtro ativo)"

### 14.2 — Background Scan com SSE
Substituir o polling de 1.5s por Server-Sent Events:

Backend: novo endpoint `GET /api/scan/stream` que emite eventos:
```
data: {"type": "progress", "percent": 42, "current_file": "UserService.java"}
data: {"type": "completed", "nodes": 3871, "edges": 12043}
```

Frontend: usar `EventSource` para receber updates em tempo real, eliminando polling.

### 14.3 — Cache Inteligente de Análises
Não re-calcular métricas que não mudaram:

Backend: hash do arquivo (SHA256) armazenado por node_key no SQLite
- Se hash não mudou desde último scan: reutilizar métricas existentes
- Somente reprocessar arquivos modificados
- Endpoint `GET /api/scan/cache-stats` mostrando hit rate

---

## RESTRIÇÕES TÉCNICAS (manter obrigatoriamente):
- 100% local/offline (exceto OSV.dev para OSS exposure)
- Backend Python/FastAPI, zero novas dependências pesadas
- Frontend React + TypeScript + Tailwind, SVG puro para charts
- Compatível com Neo4j offline E modo in-memory
- UI responde em < 200ms para operações locais
- Grafo navegável com até 5000 nós

## ORDEM DE IMPLEMENTAÇÃO SUGERIDA:
```
Sprint 10: 10.1 + 10.2 (Security Dashboard)
Sprint 11: 13.2 + 13.1 (TODO scanner + Git blame)
Sprint 12: 11.1 + 11.3 (Compare projects + Health score)
Sprint 13: 12.1 + 12.2 (Refactoring suggestions + Pattern detector)
Sprint 14: 14.1 + 14.2 (Lazy loading + SSE)
Sprint 15: 11.2 + 12.3 + 14.3 (Cross-project graph + Predictor + Cache)
Implemente fase por fase, começando pela 10.1. Sempre integre ao App.tsx e mantenha o dark theme com CSS variables do index.css.